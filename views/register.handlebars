<div class="container">
    {{> img}}
    <h1>Make Godzilla Feared Again</h1>
</div>
    <h2>Please register an account to support the petition or <a href="/login">log in here</a></h2>

    {{#if error}}
    <div id="error"><h3>⛔️ Please fill out all fields to register an account</h3></div>
    {{/if}}

    <form method="POST">
        <input name="first" type="text" placeholder="First name" autocomplete="off">
        <input name="last" type="text" placeholder="Last name" autocomplete="off">
        <input name="email" type="text" placeholder="Email address" autocomplete="off">
        <input name="pw" type="password" placeholder="Password" autocomplete="off">
        <!-- Always add the following line when using CSRF middleware -->
        <input type="hidden" name="_csrf" value="{{csrfToken}}">
        <button id="button">Submit</button>
    </form>